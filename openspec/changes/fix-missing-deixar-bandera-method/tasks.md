## 1. Implementation

- [x] 1.1 Add `public void DeixarDeSeguir()` to `Bandera.cs`.
- [x] 1.2 Implement `transform.SetParent(null);` inside the new method.

## 2. Verification

- [x] 2.1 Verify the CS1061 compilation error is resolved.
- [x] 2.2 Verify that other scripts can now call `DeixarDeSeguir()` without errors.
