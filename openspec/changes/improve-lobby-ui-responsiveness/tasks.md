## 1. Style Centralization (USS)

- [x] 1.1 Add the `.finestra-principal` class to `DAMT3Atrapa la bandera/Assets/UI/MenuStyles.uss` with `width: 80%`, `max-width: 600px`, `height: auto`, and centered alignment.
- [x] 1.2 Add general button styling to `MenuStyles.uss` with `font-size: 16px`.

## 2. UI Structure and Responsive Layout (UXML)

- [x] 2.1 Update `finestraLogin` in `MenuUI.uxml`: Apply `.finestra-principal` class and remove fixed width/height inline styles.
- [x] 2.2 Update `finestraLobby` in `MenuUI.uxml`: Apply `.finestra-principal` class and remove fixed width/height inline styles.
- [x] 2.3 Update `caixaPopUp` in `MenuUI.uxml`: Apply `.finestra-principal` class and remove fixed width/height inline styles.
- [x] 2.4 Update `finestraEspera` in `MenuUI.uxml`: Apply `.finestra-principal` class and remove fixed width/height inline styles.
- [x] 2.5 Ensure the room code label exists in the `pantallaSalaEspera` section of `MenuUI.uxml` and has `name="codeLabel"`.
- [x] 2.6 Review and increase `font-size` to at least 16px for all critical labels and buttons in `MenuUI.uxml`.

## 3. Verification

- [x] 3.1 Validate the `.uxml` syntax is correct after manual edits.
- [x] 3.2 Confirm that the `.uss` file is correctly linked and classes are applied.
