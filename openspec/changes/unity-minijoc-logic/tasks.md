## 1. Preparation

- [x] 1.1 Verify the target directory `Assets/Scripts/` exists and is correctly located within the Unity project.

## 2. Rock-Paper-Scissors-Lizard-Spock (RPSTLLS)

- [x] 2.1 Review the existing `MinijocPPTLLS.cs` to ensure it is independent of `MonoBehaviour`.
- [x] 2.2 Verify that the logic implements all 10 rules as specified in `specs.md`.

## 3. New Minigames Implementation

- [x] 3.1 Create `MinijocParellsSenarsLogic.cs` for the Evens or Odds logic.
- [x] 3.2 Create `MinijocAcaparamentMiradesLogic.cs` for the Gaze Snatching logic.
- [x] 3.3 Create `MinijocPolsimForcaLogic.cs` for the Force Dust progression logic.
- [x] 3.4 Create `MinijocAturaBarraLogic.cs` for the timed Bar Stopping logic.
- [x] 3.5 Create `MinijocCablePelatLogic.cs` for the reflex-based Cable Stripping logic.

## 4. Final Verification

- [x] 4.1 Confirm that all new scripts do NOT inherit from `MonoBehaviour`.
- [x] 4.2 Verify that all logic uses standard C# types and is fully decoupled from Unity-specific lifecycle methods.
